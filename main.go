package main

func main() {
	// start with map of device IDs to clients
	// findIPs(clientMap, nil)
	// fmt.Println(clientMap["6COWOOR-PAATSRB-I36VNI7-SUM2RR5-ONENLNB-P7QHO4X-P2QIC22-RGII2AV"])
	// Fill in the IP addresses of the map (and nicknames!)
	// each client creates a device which will be accessible through the client map
	// ask each client for its folders. Create a device web for each folder.

}
